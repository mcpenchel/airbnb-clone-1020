<h1>Flats</h1>

<% @flats.each do |flat| %>
  <div>
    <h2><%= link_to flat['name'], flat_path(flat['id']) %></h2>
    <img src="<%= flat['imageUrl'] %>" alt="" width=100 height=100>
  </div>
<% end %>
